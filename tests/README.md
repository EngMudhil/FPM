# Shared / cross-package tests

Application unit tests live next to source (e.g. `apps/web/src/**/*.test.ts`).
E2E tests live under `apps/web/e2e/`.

Use this directory for future cross-cutting integration tests.
